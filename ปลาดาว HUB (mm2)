local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RUNNING = true

_G.ESP_ENABLED = false
_G.AIM_ENABLED = false
_G.AVATAR_ENABLED = false
_G.AUTO_COIN = false 
_G.FLING_ACTIVE = false 
_G.SPEED_JUMP_ACTIVE = false
_G.PLAYER_SPEED = 16
_G.PLAYER_JUMP = 50
_G.SPEED = 25
_G.FLING_POWER = 1000000 
_G.HEIGHT = 3
_G.DETECTION_RANGE = 150

local isUILocked = false
local RoleColors = {
    Murderer = Color3.fromRGB(255, 65, 65),    
    Sheriff = Color3.fromRGB(65, 200, 255),   
    Innocent = Color3.fromRGB(65, 255, 150)    
}

local function tween(obj, props, time)
    TweenService:Create(obj, TweenInfo.new(time or 0.3, Enum.EasingStyle.Exponential, Enum.EasingDirection.Out), props):Play()
end

local function applySpeedJump()
    pcall(function()
        local char = LocalPlayer.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then
            if _G.SPEED_JUMP_ACTIVE then
                hum.WalkSpeed = _G.PLAYER_SPEED
                hum.UseJumpPower = true
                hum.JumpPower = _G.PLAYER_JUMP
            else
                hum.WalkSpeed = 16
                hum.JumpPower = 50
            end
        end
    end)
end

task.spawn(function()
    while task.wait(0.1) do
        if not RUNNING then break end
        applySpeedJump()
    end
end)

local gui = Instance.new("ScreenGui", CoreGui)
gui.Name = "Starfish_V3_Integrated"

local Main = Instance.new("Frame", gui)
Main.Size = UDim2.new(0, 200, 0, 270) 
Main.Position = UDim2.new(0, 15, 0.5, -135)
Main.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
Main.BorderSizePixel = 0 
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 12)

local Glow = Instance.new("UIStroke", Main)
Glow.Color = Color3.fromRGB(255, 110, 50)
Glow.Thickness = 1.5
Glow.Transparency = 0.6

local SideTab = Instance.new("TextButton", Main)
SideTab.Size = UDim2.new(0, 24, 0, 50)
SideTab.Position = UDim2.new(1, 4, 0.5, -25)
SideTab.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
SideTab.Text = "★"
SideTab.TextColor3 = Color3.fromRGB(255, 110, 50)
SideTab.Font = Enum.Font.GothamBold
SideTab.TextSize = 16
SideTab.BorderSizePixel = 0
Instance.new("UICorner", SideTab).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", SideTab).Color = Color3.fromRGB(255, 110, 50)

local TopBar = Instance.new("Frame", Main)
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundTransparency = 1

local Title = Instance.new("TextLabel", TopBar)
Title.Size = UDim2.new(1, 0, 1, 0)
Title.Text = "STARFISH HUB"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBlack
Title.TextSize = 13
Title.BackgroundTransparency = 1
local TitleGradient = Instance.new("UIGradient", Title)
TitleGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 180, 100)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 80, 20))
}

local Scroll = Instance.new("ScrollingFrame", Main)
Scroll.Size = UDim2.new(1, 0, 1, -85)
Scroll.Position = UDim2.new(0, 0, 0, 40)
Scroll.BackgroundTransparency = 1
Scroll.CanvasSize = UDim2.new(0, 0, 0, 620) 
Scroll.ScrollBarThickness = 0

local List = Instance.new("UIListLayout", Scroll)
List.HorizontalAlignment = Enum.HorizontalAlignment.Center
List.Padding = UDim.new(0, 6)
List.SortOrder = Enum.SortOrder.LayoutOrder

local TPFrame = Instance.new("Frame", gui)
TPFrame.Size = UDim2.new(0, 160, 0, 200)
TPFrame.Position = UDim2.new(0, 225, 0.5, -100)
TPFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
TPFrame.Visible = false
Instance.new("UICorner", TPFrame).CornerRadius = UDim.new(0, 10)
local TPS = Instance.new("UIStroke", TPFrame)
TPS.Color = Color3.fromRGB(255, 110, 50)
TPS.Transparency = 0.5

local TPScroll = Instance.new("ScrollingFrame", TPFrame)
TPScroll.Size = UDim2.new(1, -10, 1, -10)
TPScroll.Position = UDim2.new(0, 5, 0, 5)
TPScroll.BackgroundTransparency = 1
TPScroll.ScrollBarThickness = 0
Instance.new("UIListLayout", TPScroll).Padding = UDim.new(0, 4)

function updatePlayerList()
    for _, v in pairs(TPScroll:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local b = Instance.new("TextButton", TPScroll)
            b.Size = UDim2.new(1, 0, 0, 28)
            b.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
            b.Text = "  " .. p.DisplayName
            b.TextColor3 = Color3.fromRGB(255, 255, 255)
            b.Font = Enum.Font.GothamMedium
            b.TextSize = 10
            b.TextXAlignment = Enum.TextXAlignment.Left
            b.BorderSizePixel = 0
            Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)
            b.MouseButton1Click:Connect(function()
                if p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                    LocalPlayer.Character.HumanoidRootPart.CFrame = p.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                end
            end)
        end
    end
end

local function createToggle(name, default, callback, order)
    local Btn = Instance.new("TextButton", Scroll)
    Btn.Size = UDim2.new(1, -20, 0, 34)
    Btn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    Btn.Text = "  " .. name
    Btn.TextColor3 = Color3.fromRGB(230, 230, 230)
    Btn.Font = Enum.Font.GothamSemibold
    Btn.TextSize = 10
    Btn.TextXAlignment = Enum.TextXAlignment.Left
    Btn.LayoutOrder = order
    Btn.BorderSizePixel = 0
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 8)
    
    local Sw = Instance.new("Frame", Btn)
    Sw.Size = UDim2.new(0, 30, 0, 14)
    Sw.Position = UDim2.new(1, -40, 0.5, -7)
    Sw.BackgroundColor3 = default and Color3.fromRGB(255, 110, 50) or Color3.fromRGB(45, 45, 50)
    Instance.new("UICorner", Sw).CornerRadius = UDim.new(1, 0)

    local Circle = Instance.new("Frame", Sw)
    Circle.Size = UDim2.new(0, 10, 0, 10)
    Circle.Position = default and UDim2.new(1, -12, 0.5, -5) or UDim2.new(0, 2, 0.5, -5)
    Circle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", Circle).CornerRadius = UDim.new(1, 0)
    
    local state = default
    Btn.MouseButton1Click:Connect(function()
        if isUILocked then return end
        state = not state
        callback(state)
        tween(Sw, {BackgroundColor3 = state and Color3.fromRGB(255, 110, 50) or Color3.fromRGB(45, 45, 50)})
        tween(Circle, {Position = state and UDim2.new(1, -12, 0.5, -5) or UDim2.new(0, 2, 0.5, -5)})
    end)
end

local function createSlider(name, min, max, default, callback, order)
    local Frame = Instance.new("Frame", Scroll)
    Frame.Size = UDim2.new(1, -20, 0, 48)
    Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    Frame.LayoutOrder = order
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    
    local Label = Instance.new("TextLabel", Frame)
    Label.Size = UDim2.new(1, -20, 0, 20)
    Label.Position = UDim2.new(0, 10, 0, 4)
    Label.Text = name .. " : " .. tostring(default)
    Label.TextColor3 = Color3.fromRGB(230, 230, 230)
    Label.Font = Enum.Font.GothamSemibold
    Label.TextSize = 9
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.BackgroundTransparency = 1

    local SliderBg = Instance.new("Frame", Frame)
    SliderBg.Size = UDim2.new(1, -20, 0, 4)
    SliderBg.Position = UDim2.new(0, 10, 0, 32)
    SliderBg.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    Instance.new("UICorner", SliderBg).CornerRadius = UDim.new(1, 0)

    local Fill = Instance.new("Frame", SliderBg)
    Fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    Fill.BackgroundColor3 = Color3.fromRGB(255, 110, 50)
    Instance.new("UICorner", Fill).CornerRadius = UDim.new(1, 0)
    
    local Btn = Instance.new("TextButton", SliderBg)
    Btn.Size = UDim2.new(1, 0, 1, 15)
    Btn.Position = UDim2.new(0, 0, 0.5, -7)
    Btn.BackgroundTransparency = 1
    Btn.Text = ""

    local dragging = false
    local inputObj = nil

    Btn.InputBegan:Connect(function(input)
        if isUILocked then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true; inputObj = input; Scroll.ScrollingEnabled = false
        end
    end)
    UIS.InputEnded:Connect(function(input) if input == inputObj then dragging = false; inputObj = nil; Scroll.ScrollingEnabled = true end end)
    
    RunService.RenderStepped:Connect(function()
        if dragging and inputObj and not isUILocked then
            local pos = math.clamp((inputObj.Position.X - SliderBg.AbsolutePosition.X) / SliderBg.AbsoluteSize.X, 0, 1)
            local value = math.floor(min + ((max - min) * pos))
            Fill.Size = UDim2.new(pos, 0, 1, 0)
            Label.Text = name .. " : " .. tostring(value)
            callback(value)
        end
    end)
end

createToggle("ESP Highlight", _G.ESP_ENABLED, function(s) _G.ESP_ENABLED = s end, 1)
createToggle("Aimbot Murder", _G.AIM_ENABLED, function(s) _G.AIM_ENABLED = s end, 2)
createToggle("Show Avatar Icon", _G.AVATAR_ENABLED, function(s) _G.AVATAR_ENABLED = s end, 3)
createToggle("Auto เก็บเหรียญ", _G.AUTO_COIN, function(s) _G.AUTO_COIN = s end, 4)
createSlider("ความเร็วเก็บเหรียญ", 16, 100, _G.SPEED, function(v) _G.SPEED = v end, 5)
createToggle("Stable Fling (เดินชน)", _G.FLING_ACTIVE, function(s) _G.FLING_ACTIVE = s end, 6)
createSlider("Fling Power", 5000, 2000000, _G.FLING_POWER, function(v) _G.FLING_POWER = v end, 7)
createToggle("Teleport List", false, function(s) 
    TPFrame.Visible = s 
    if s then updatePlayerList() end 
end, 8)

createToggle("เปิดใช้ Speed / Jump", _G.SPEED_JUMP_ACTIVE, function(s) 
    _G.SPEED_JUMP_ACTIVE = s 
    applySpeedJump()
end, 9)
createSlider("ความเร็วตัวละคร (WalkSpeed)", 16, 200, _G.PLAYER_SPEED, function(v) 
    _G.PLAYER_SPEED = v 
    applySpeedJump()
end, 10)
createSlider("แรงกระโดด (JumpPower)", 50, 300, _G.PLAYER_JUMP, function(v) 
    _G.PLAYER_JUMP = v 
    applySpeedJump()
end, 11)

local Footer = Instance.new("Frame", Main)
Footer.Size = UDim2.new(1, 0, 0, 45)
Footer.Position = UDim2.new(0, 0, 1, -45)
Footer.BackgroundTransparency = 1

local LockBtn = Instance.new("TextButton", Footer)
LockBtn.Size = UDim2.new(0, 30, 0, 30)
LockBtn.Position = UDim2.new(0, 10, 0.5, -15)
LockBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
LockBtn.Text = "🔓"
LockBtn.TextSize = 14
LockBtn.BorderSizePixel = 0
Instance.new("UICorner", LockBtn).CornerRadius = UDim.new(0, 8)
LockBtn.MouseButton1Click:Connect(function()
    isUILocked = not isUILocked
    LockBtn.Text = isUILocked and "🔒" or "🔓"
    tween(LockBtn, {BackgroundColor3 = isUILocked and Color3.fromRGB(255, 110, 50) or Color3.fromRGB(25, 25, 30)})
end)

local Exit = Instance.new("TextButton", Footer)
Exit.Size = UDim2.new(1, -60, 0, 30) 
Exit.Position = UDim2.new(0, 50, 0.5, -15)
Exit.BackgroundColor3 = Color3.fromRGB(40, 20, 20)
Exit.Text = "UNINJECT"
Exit.TextColor3 = Color3.fromRGB(255, 100, 100)
Exit.Font = Enum.Font.GothamBold
Exit.TextSize = 10
Exit.BorderSizePixel = 0
Instance.new("UICorner", Exit).CornerRadius = UDim.new(0, 8)
Exit.MouseButton1Click:Connect(function() RUNNING = false; gui:Destroy() end)

local function getRole(p)
    if not p or not p.Character then return "Innocent" end
    local char = p.Character; local backpack = p:FindFirstChild("Backpack")
    local knife = char:FindFirstChild("Knife") or char:FindFirstChild("Dagger") or (backpack and (backpack:FindFirstChild("Knife") or backpack:FindFirstChild("Dagger")))
    local gun = char:FindFirstChild("Gun") or char:FindFirstChild("Revolver") or (backpack and (backpack:FindFirstChild("Gun") or backpack:FindFirstChild("Revolver")))
    if knife then return "Murderer" end
    if gun then return "Sheriff" end
    return "Innocent"
end

local function getClosestCoin(hrp)
    local closest, dist = nil, _G.DETECTION_RANGE
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") and (v.Name:find("Coin") or v:FindFirstChild("CoinVisual")) and v.Transparency < 1 then
            local d = (hrp.Position - v.Position).Magnitude
            if d < dist then dist = d; closest = v end
        end
    end
    return closest
end

local bv
local function handleAutoCoin(myChar, myHrp)
    if not _G.AUTO_COIN then
        if bv then bv:Destroy(); bv = nil end
        return
    end
    if not bv then bv = Instance.new("BodyVelocity", myHrp); bv.MaxForce = Vector3.new(1e6, 1e6, 1e6) end
    
    for _, v in pairs(myChar:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end
    
    local coin = getClosestCoin(myHrp)
    if coin then
        local target = coin.Position + Vector3.new(0, _G.HEIGHT, 0)
        bv.Velocity = (target - myHrp.Position).Unit * _G.SPEED
        pcall(function() firetouchinterest(myHrp, coin, 0); firetouchinterest(myHrp, coin, 1) end)
    else 
        if bv then bv.Velocity = Vector3.zero end
    end
end

RunService.RenderStepped:Connect(function()
    if not RUNNING then return end
    local myChar = LocalPlayer.Character
    local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
    local targetMurd = nil
    
    for _, p in pairs(Players:GetPlayers()) do
        pcall(function()
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local char = p.Character; local role = getRole(p)
                
                local hl = char:FindFirstChild("SF_Visual")
                if _G.ESP_ENABLED then
                    if not hl then hl = Instance.new("Highlight", char); hl.Name = "SF_Visual" end
                    hl.FillColor = RoleColors[role]; hl.OutlineColor = RoleColors[role]
                elseif hl then hl:Destroy() end
                
                local head = char:FindFirstChild("Head")
                if head then
                    local icon = head:FindFirstChild("StarIcon")
                    if _G.AVATAR_ENABLED then
                        if not icon then
                            icon = Instance.new("BillboardGui", head); icon.Name = "StarIcon"
                            icon.Size = UDim2.new(0, 30, 0, 30); icon.StudsOffset = Vector3.new(0, 3, 0); icon.AlwaysOnTop = true
                            local img = Instance.new("ImageLabel", icon); img.Size = UDim2.new(1,0,1,0); img.BackgroundTransparency = 1
                            img.Image = "rbxthumb://type=AvatarHeadShot&id="..p.UserId.."&w=150&h=150"
                            Instance.new("UICorner", img).CornerRadius = UDim.new(1,0)
                            local st = Instance.new("UIStroke", img); st.Thickness = 2; st.Name = "St"
                        end
                        icon.ImageLabel.St.Color = RoleColors[role]
                    elseif icon then icon:Destroy() end
                end

                if role == "Murderer" then targetMurd = char end
            end
        end)
    end
    
    if _G.AIM_ENABLED and targetMurd and myChar and (myChar:FindFirstChild("Gun") or myChar:FindFirstChild("Revolver")) then
        pcall(function()
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetMurd.HumanoidRootPart.Position)
        end)
    end
    
    if myHrp then handleAutoCoin(myChar, myHrp) end
end)

RunService.Heartbeat:Connect(function()
    if RUNNING and _G.FLING_ACTIVE and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local root = LocalPlayer.Character.HumanoidRootPart
        local oldV = root.Velocity
        root.Velocity = Vector3.new(_G.FLING_POWER, _G.FLING_POWER, _G.FLING_POWER)
        RunService.RenderStepped:Wait()
        root.Velocity = oldV
    end
end)

local function makeDraggable(obj, header)
    local activeDragInput = nil; local startPos; local dragStart
    header.InputBegan:Connect(function(i) if not isUILocked and (i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch) then activeDragInput = i; dragStart = i.Position; startPos = obj.Position end end)
    UIS.InputChanged:Connect(function(i) if i == activeDragInput then local d = i.Position - dragStart; obj.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + d.X, startPos.Y.Scale, startPos.Y.Offset + d.Y) end end)
    UIS.InputEnded:Connect(function(i) if i == activeDragInput then activeDragInput = nil end end)
end
makeDraggable(Main, TopBar)

local isCol = false
SideTab.MouseButton1Click:Connect(function()
    isCol = not isCol
    tween(Main, {Position = isCol and UDim2.new(0, -205, Main.Position.Y.Scale, Main.Position.Y.Offset) or UDim2.new(0, 15, Main.Position.Y.Scale, Main.Position.Y.Offset)})
    tween(SideTab, {Rotation = isCol and 180 or 0})
end)

Players.PlayerAdded:Connect(updatePlayerList)
Players.PlayerRemoving:Connect(updatePlayerList)
updatePlayerList()
